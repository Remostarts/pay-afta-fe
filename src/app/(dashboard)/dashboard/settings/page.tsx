import Settings from '@/components/view/dashboard/Settings';

export default function Page() {
  return (
    <section className="min-h-screen w-full rounded-md bg-gray-50">
      <Settings />
    </section>
  );
}
